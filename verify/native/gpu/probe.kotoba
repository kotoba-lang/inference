(ns gpu.probe (:export [info]))
;; :gpu/compute (wire 42) smoke probe: what device answers, as the loader's
;; INFO string "<device>|<api>|<maxWgX>|<unified>". Root ADR-2609182400 C.
(defn info [] :string
  (typed-cap-call :gpu/compute :string :string "INFO"))
