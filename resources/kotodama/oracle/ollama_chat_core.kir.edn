{:format :kotoba.kir/v4,
 :signature nil,
 :functions
 [{:name role-unknown,
   :params [],
   :result :i64,
   :effects #{},
   :body 0,
   :param-types []}
  {:name role-system,
   :params [],
   :result :i64,
   :effects #{},
   :body 1,
   :param-types []}
  {:name role-user,
   :params [],
   :result :i64,
   :effects #{},
   :body 2,
   :param-types []}
  {:name role-assistant,
   :params [],
   :result :i64,
   :effects #{},
   :body 3,
   :param-types []}
  {:name role-tool,
   :params [],
   :result :i64,
   :effects #{},
   :body 4,
   :param-types []}
  {:name role-code,
   :params [name],
   :result :i64,
   :effects #{},
   :body
   (if
    (string=? name "system")
    1
    (if
     (string=? name "user")
     2
     (if
      (string=? name "assistant")
      3
      (if (string=? name "tool") 4 0)))),
   :param-types [:string]}
  {:name role-valid?,
   :params [code],
   :result :bool,
   :effects #{},
   :body
   (let
    [__kotoba_and_2 (> code 0)]
    (if __kotoba_and_2 (< code 5) __kotoba_and_2)),
   :param-types [:i64]}
  {:name role-wire-name,
   :params [code],
   :result :string,
   :effects #{},
   :body
   (if
    (= code 1)
    "system"
    (if
     (= code 2)
     "user"
     (if (= code 3) "assistant" (if (= code 4) "tool" "")))),
   :param-types [:i64]}
  {:name admissible-final-role?,
   :params [code],
   :result :bool,
   :effects #{},
   :body
   (let
    [__kotoba_or_2 (= code 2)]
    (if __kotoba_or_2 __kotoba_or_2 (= code 4))),
   :param-types [:i64]}
  {:name assistant-wire-role,
   :params [],
   :result :string,
   :effects #{},
   :body "assistant",
   :param-types []}],
 :oracle-value nil,
 :schema-identities nil,
 :exports
 [role-unknown
  role-system
  role-user
  role-assistant
  role-tool
  role-code
  role-valid?
  role-wire-name
  admissible-final-role?
  assistant-wire-role],
 :schemas nil,
 :effects #{},
 :blocks [],
 :entry nil}
